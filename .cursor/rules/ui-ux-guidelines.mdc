---
description: Zentee 前端 UI/UX、可访问性、响应式与视觉一致性规则
globs: front/**/*.{ts,tsx,css}
alwaysApply: false
---

# UI/UX 设计规则

## 1. 品牌方向

- Zentee 视觉关键词：Craft / Pure / Mountain / Origin / Ritual。
- UI 面向用户必须使用英文；文案语气克制、清晰、可信，避免夸张营销话术。
- 优先使用现有 Tailwind 令牌：`brand.*`（茶绿）、`sage.*`（自然灰绿）、`cream.*`（暖底色）、`grey.*`（中性色）。
- 新增颜色、阴影、圆角、字号前，先检查 `front/tailwind.config.js`；不要在组件里散落一次性 hex 色值。

## 2. 视觉层级

- 页面应有明确主行动：每屏优先 1 个 primary CTA，次要操作用 secondary/ghost/link。
- 标题层级保持语义与视觉一致：每页 1 个主 `h1`，模块用 `h2/h3`，不要靠字号伪造层级。
- 使用 8pt spacing 思路：常用间距优先 `2/3/4/5/6/8/10/12`，避免随意数值。
- 卡片和区块应靠留白、边框、柔和阴影建立层次；不要堆叠重阴影、高饱和背景或过多渐变。

## 3. 组件一致性

- 新 UI 优先复用 `front/src/modules/common/**`、Medusa UI 与既有 `modules/<domain>` 组件。
- 组件必须覆盖常见状态：default、hover、focus-visible、disabled、loading、empty、error。
- 商品、订单、表单、筛选等业务 UI 应放在对应 `modules/<domain>/components|templates`，页面文件只负责取数和组装。
- 不为单一页面复制一套基础控件；确有差异时先扩展 props 或组合现有组件。

## 4. 响应式与布局

- 移动端优先设计；断点沿用项目配置：`2xsmall`、`xsmall`、`small`、`medium`、`large`、`xlarge`、`2xlarge`。
- 列表/网格在窄屏避免横向溢出；筛选、导航、购物车等复杂面板优先 drawer / collapsible。
- 商品图片保持稳定比例和尺寸占位，避免 CLS；长文本必须有合理换行、截断或展开策略。
- 重要购买路径（商品详情 → 加购 → 购物车 → 结账）在移动端必须可单手完成，CTA 不应被遮挡。

## 5. 可访问性（WCAG AA）

- 文本与背景对比度至少 AA；浅 `cream` 背景上不要使用过浅的 `sage` 文本。
- 所有可交互元素必须键盘可达，并有可见 `focus-visible` 状态。
- 图标按钮需要 `aria-label`；表单控件要有 label、错误文案和语义关联。
- 图片必须有有意义的 `alt`；装饰图使用空 `alt=""`。
- 不用颜色作为唯一状态提示；错误、成功、库存、筛选状态需配合文本或图标。

## 6. 交互与动效

- 动效优先 150–200ms，使用 `ease-out` 或 `ease-in-out`；只用于反馈、空间关系或状态变化。
- 避免大面积、循环、高频动画；尊重 `prefers-reduced-motion`，关键操作不依赖动画完成。
- 表单提交、加购、支付等动作必须有明确 loading / disabled 防重复提交状态。
- 错误提示应说明问题和下一步，不只显示 “Something went wrong”。

## 7. 电商转化体验

- 商品卡片优先展示：清晰图片、产品名、价格、茶类/等级/产地等关键信息。
- 商品详情优先展示：图片、价格、规格选择、库存/配送信号、加购 CTA、Quick Brew。
- 茶叶内容（Origin、Brewing Guide、Brew Tips）应服务购买决策，不要喧宾夺主挤压加购路径。
- 空状态要可行动：购物车空、无筛选结果、订单为空时提供返回店铺或调整筛选的入口。

## 8. 性能与实现

- 优先 Server Component；只有交互状态、浏览器 API、事件处理才使用 Client Component。
- 避免为小交互引入重依赖；图片使用 Next/Image 或既有图片组件，并设置尺寸/懒加载策略。
- Tailwind class 保持可读，复杂样式抽组件或小工具函数，不在页面堆长 class 复制。
- UI 修改完成后至少检查：移动端、桌面端、键盘导航、loading/empty/error 状态。

## 9. 设计原则（业界通用）

- 可用性优先于装饰：用户应能在 3 秒内理解当前页面是什么、能做什么、下一步去哪。
- 识别优先于记忆：关键状态、筛选条件、价格、规格、配送信息要可见，不让用户靠记忆前一步。
- 渐进披露：复杂茶知识、产地故事、冲泡细节可折叠或分区展示；购买关键路径始终靠前。
- 一致性优先：同类按钮、卡片、表单、筛选项在不同页面保持同样布局、命名和交互。
- 容错设计：破图、无库存、API 失败、空列表、未登录、支付失败都要有可恢复路径。

## 10. 信息架构

- 页面顶部优先回答：我在哪里、这是什么、为什么可信、我能做什么。
- 导航层级不超过 2 层；移动端菜单分组清晰，避免一次展示过多链接。
- 面包屑用于商品、集合、分类、指南、产地详情页；链接文本应表达目标页面，不使用 “Click here”。
- 列表页优先提供可扫读信息：标题、短说明、筛选、排序、结果数量、分页/加载状态。
- 内容页使用“摘要在前、细节在后”：Origin/Guide 页先给核心结论，再展开故事与长内容。

## 11. 导航、搜索与筛选

- 导航中的 Cart、Account、Country/Region 入口保持固定位置；不要在不同页面改变主导航结构。
- 当前页面或当前分类应有 active 状态；active 不只靠颜色区分。
- 筛选项应可撤销，已选条件要显示在列表上方或筛选面板内。
- 无结果时展示原因和建议：清空筛选、返回全部商品、查看热门茶类。
- 排序/筛选改变 URL query，保证可分享、可返回、刷新后状态不丢失。

## 12. 表单与结账体验

- 表单按用户心智分组：Contact → Shipping → Delivery → Payment → Review。
- 输入框 label 不只依赖 placeholder；placeholder 只放示例，不放必读说明。
- 校验尽量就近、即时、可修复；错误文案说明“哪里错 + 如何改”。
- 结账页减少干扰：避免大导航、复杂营销模块和离开结账的强 CTA。
- 支付、下单、加购按钮提交中必须 disabled，并保留清晰 loading 文案。
- 地址、配送、支付信息变更后，订单总价和配送费用要有明确刷新/更新反馈。

## 13. 商品与茶叶内容展示

- 商品卡片图片比例统一；同一列表中价格、标题、标签位置固定，减少视觉跳动。
- 商品标题避免全大写；茶类、等级、产地、风味标签用短标签，不堆长句。
- 商品详情的首屏优先级：图库 → 标题/价格 → 规格 → 库存/配送 → 加购 → Quick Brew。
- 相关商品要解释关联原因：same origin、same tea type、similar flavor、popular with this tea。
- 冲泡指南参数用结构化卡片展示：water temp、leaf ratio、steep time、brew count；长步骤再放下方。
- 产地内容需突出可感知价值：flavor profile、altitude、climate、代表茶，而不只是故事文本。

## 14. 文案与微交互文案

- 英文 UI 文案使用 sentence case，按钮以动词开头：`Add to cart`、`View guide`、`Clear filters`。
- CTA 要具体，不使用泛泛的 `Submit`、`Continue`；结账中可用 `Continue to delivery` 等上下文文案。
- 错误文案避免责备用户；用行动导向：`Enter a valid postal code`。
- 空状态文案包含下一步：`No teas match these filters. Clear filters or browse all teas.`。
- 成功反馈短而明确：`Added to cart`、`Address saved`、`Payment authorized`。

## 15. 颜色、暗色模式与语义状态

- 颜色用途固定：`brand` 用于主行动与品牌强调；`sage` 用于自然中性色；`cream` 用于暖背景；`grey` 用于正文/边框。
- 语义状态不要直接随机选色：成功、警告、错误、信息若项目未定义 token，优先用 Medusa UI 组件或补充 token。
- 暗色模式下不要简单反转颜色；检查文字、边框、卡片、阴影、图片遮罩和 disabled 状态。
- 链接需有 hover/focus/active 状态；正文内链接不只靠颜色，建议有 underline 或清晰 affordance。

## 16. 图片、媒体与内容质量

- 商品图要优先清晰、真实、背景统一；避免同一列表中混用大幅不同色温/裁切风格。
- Hero 图必须保证文字可读：必要时使用渐变遮罩，但遮罩不要过重。
- 使用远景山水/茶园图表达 Origin；使用近景茶叶/茶汤图表达 Product；使用步骤图表达 Guide。
- 图片组件要设置尺寸或容器比例；列表和详情页避免加载原始超大图。
- `alt` 应描述内容和用途，例如 `Loose leaf oolong tea from Fujian`，不要写 `image`。

## 17. 设计评审清单

- 首屏是否有清晰主行动，且移动端无需横向滚动？
- 颜色、字号、圆角、阴影是否来自现有 token 或现有组件？
- Hover、focus-visible、disabled、loading、empty、error 是否齐全？
- 键盘能否完成主要流程：打开菜单、筛选、选择规格、加购、结账？
- 文案是否具体、英文一致、错误可修复？
- 图片是否有稳定尺寸、合理 `alt`，不会导致 CLS？
- 商品详情是否没有让内容模块抢走加购路径？
- 修改是否复用了 `modules/common`、Medusa UI 或现有 domain 组件？

## 18. 审美原则

- 整体气质：安静、克制、自然、有呼吸感；避免廉价促销感、过度装饰和模板化电商噪音。
- 审美关键词：warm minimalism、botanical elegance、quiet premium、ritual calm、origin craft。
- 留白比装饰更重要：高端感来自空间、比例、文字节奏和图片质量，不来自堆叠边框、阴影、图标和渐变。
- 色彩应低饱和、自然过渡；`brand` 用于关键强调，`cream` 用于温暖背景，`sage` 用于柔和层次，避免大片高饱和绿色。
- 页面节奏要有“疏密”：Hero/故事区留白充足，商品网格信息紧凑但不拥挤，长内容用分段和卡片建立呼吸。
- 字体搭配：标题可使用 `font-display` 营造品牌感，正文保持 `font-sans` 清晰可读；不要在同一模块混用过多字重、字号和大小写风格。
- 图片审美优先真实质感：茶叶纹理、茶汤、器具、山地/产地环境；避免塑料感库存图、过曝白底图和强滤镜。
- 卡片审美应轻：细边框、柔和背景、微阴影即可；不要让每张卡片都像独立广告牌。
- 圆角与阴影要统一：优先 `rounded` / `large`，阴影只用于层级，不用于制造“炫技”。
- 品牌装饰元素要少而准：叶片、山形、冲泡线条等只作局部点缀，不铺满背景。
- 视觉焦点每屏最多 1-2 个；如果所有元素都在强调，就等于没有重点。
- 审美不牺牲转化：再高级的版式也不能降低规格选择、价格、库存、加购按钮的可见性。

## 19. Zentee 视觉反模式

- 不使用大面积荧光绿、纯黑重压背景、强玻璃拟态、霓虹描边、夸张 3D 图标。
- 不把茶文化做成“古风堆料”：避免过多卷轴、印章、毛笔字、金色纹样。
- 不使用密集促销组件：倒计时、满屏 badge、连续弹窗、强闪烁动画。
- 不让文字压在复杂图片上；必要时使用柔和渐变遮罩，并保证 AA 对比度。
- 不为“高级感”牺牲可读性：正文不使用过小字号、过低对比、过宽行长或过细字重。
- 不在商品列表中混用太多视觉样式：统一图片比例、卡片结构、标签样式和价格位置。

## 20. 审美验收问题

- 这个页面第一眼是否像“专业茶品牌”，而不是通用模板商城？
- 是否有足够留白让商品和内容显得珍贵，而不是拥挤廉价？
- 色彩是否自然、低噪、符合茶叶/山地/手作气质？
- 图片是否真实、有质感，并且与页面色调一致？
- 视觉重点是否服务用户决策：了解产品、信任品牌、完成购买？
- 如果移除所有装饰，页面的信息层级是否仍然成立？
